import 'package:flutter/material.dart';

class AiResultChartWidget extends StatelessWidget {
  const AiResultChartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      
    );
  }
}